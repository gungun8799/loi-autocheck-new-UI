Compare basic fields between PDF and Web sources for SERVICE EXPRESS contracts. Return JSON array of comparison results.

Compare these fields with flexible matching (allow contextual similarity, different formats):

"Building Name" - **TRUST FUND RULE**: If field "อยู่กองทรัสต์หรือไม่" is "Yes", mark as match if both contain "ซีพี" or "CP". Otherwise compare normally

"Brand Name" - ชื่อร้าน or brand name comparison

"Customer Name" - Compare customer names

"Customer Address" - Compare customer addresses normally

"Space (NLA)" - Compare area in square meters (ขนาดพื้นที่)

"Tenant Type" - Compare tenant type/category

"Unit ID" - Compare unit identifier (also called Space Id)

Flexible comparison rules:
- Match contextually similar values even if formatted differently
- Examples: "March 10, 2025" vs "10/03/2025", "$1,000.00" vs "1000", "ซีพี" vs "CP"
- Handle differences in commas, units like "THB", date formats
- If value missing in either source: mark match as false

**LR CONTRACT RULE**: For LR (renewal) contracts, apply relaxed matching:
- Customer Address: Mark as match regardless of differences (address comparison skipped for renewals)

Return format:
[
  {
    "field": "Field name",
    "pdf": "PDF value",
    "web": "Web value", 
    "match": true or false,
    "reason": "Explanation of match/mismatch"
  }
]