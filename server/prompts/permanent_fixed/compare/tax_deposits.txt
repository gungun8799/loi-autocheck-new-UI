Compare lease property tax information between PDF and Web sources. Return JSON array of comparison results.

Compare this field:

"Lease property tax rate" - Apply these validation rules with **RELAXED DECIMAL FORMATTING**:
1. Web value "PTS-PT Standard" (ignore spaces) → PDF must be "10" equivalent ("10.00", "10,00", "10.0", "10,0") to be valid
2. Web value "PT Mall" (ignore spaces) → PDF must be "12.5" equivalent ("12.5", "12,5", "12.50", "12,50") to be valid  
3. Web value "10.00" (direct numeric) → PDF must be "10" equivalent ("10.00", "10,00", "10.0", "10,0") to be valid
4. Web value "12.5" or "12.50" (direct numeric) → PDF must be "12.5" equivalent ("12.5", "12,5", "12.50", "12,50") to be valid
5. Web value null/missing → PDF must be "0" equivalent ("0", "0.0", "0,0", "0.00", "0,00") to be valid
6. **DECIMAL FORMAT FLEXIBILITY**: Accept both comma (,) and period (.) as decimal separators - compare numerical values, not exact text

Validation Logic:
- **NUMERICAL EQUIVALENCE**: Compare tax rates by numerical value, not text format
- **DECIMAL FLEXIBILITY**: "10,00" = "10.00" = "10.0" = "10,0" (all represent the value 10)
- **FORMAT TOLERANCE**: Accept both European (comma) and US (period) decimal separators
- Missing values in either source = false match

NOTE: Deposit amounts are handled in the lease_terms category, not here.

**CRITICAL: Use exact field name "Lease property tax rate" in response**

Return format:
[
  {
    "field": "Lease property tax rate", 
    "pdf": "PDF value",
    "web": "Web value",
    "match": true or false,
    "reason": "Tax rule applied: [explanation]"
  }
]

**EXAMPLE RESPONSE:**
```json
[
  {
    "field": "Lease property tax rate", 
    "pdf": "10,00",
    "web": "PTS-PT Standard",
    "match": true,
    "reason": "Tax rule applied: Web value PTS-PT Standard matches PDF value 10,00 (numerical equivalent of 10)"
  }
]
```

**CRITICAL REQUIREMENTS:**
1. ALWAYS return valid JSON array
2. ALWAYS use double quotes for all strings
3. ALWAYS use exact field name "Lease property tax rate"
4. DO NOT include any text before or after the JSON array
5. DO NOT use <think> tags or explanations