Validate deposit financial fields for PERMANENT FIXED contracts from WEB scraped data. Return JSON array of validation results.

**DEPOSIT FINANCIAL FIELDS** - Security deposits:

"Rental Deposit" - Must be present as numerical value

"Service Deposit" - Must be present as numerical value

**CONTRACT-SPECIFIC RULES:**
- **LR CONTRACT RULE**: For LR (renewal) contracts, deposits may reference previous terms
- **LO CONTRACT RULE**: For LO (new) contracts, all deposits should be explicitly stated
- Validate that financial amounts are properly extracted

**WEB-SPECIFIC RULES:**
- Web data may have different field names than PDF
- Focus on numerical value presence and formatting
- Allow for web formatting differences (currency symbols, commas, etc.)

Return format:
[
  {
    "field": "Field name",
    "value": "Web extracted value", 
    "valid": true or false,
    "reason": "Validation explanation"
  }
]